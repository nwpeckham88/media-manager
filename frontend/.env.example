# Backend API target used by Vite dev proxy
MM_API_TARGET=http://127.0.0.1:8080

# Backend runtime examples (set in shell or container env for Rust service)
# MM_LIBRARY_ROOTS=/mnt/media/movies:/mnt/media/tv
# MM_STATE_DIR=/app/.mm-state
